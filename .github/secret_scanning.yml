paths:
  # Exclude source-pinning manifests that contain git commit SHAs (not secrets).
  # These 40-character hex strings are repository revision identifiers, not credentials.
  - '!chimeralang_mcp/materials/builders.py'
